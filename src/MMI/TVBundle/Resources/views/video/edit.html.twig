{% extends '::layout.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-lg-10 col-md-10 col-sm-10 col-xs-10">
                <h1>Modification de "{{ video.title }}"</h1>

                {% form_theme edit_form "form.html.twig" %}
                {{ form_start(edit_form) }}

                <div class="form-group">
                    {# Génération du label. #}
                    {{ form_label(edit_form.title, "Titre", {'label_attr': {'class': 'control-label'}}) }}

                    {# Affichage des erreurs pour ce champ précis. #}
                    {{ form_errors(edit_form.title) }}

                    <div class="col-sm-10">
                        {# Génération de l'input. #}
                        {{ form_widget(edit_form.title, {'attr': {'class': 'form-control'}}) }}
                    </div>
                </div>

                <div class="form-group">
                    {# Génération du label. #}
                    {{ form_label(edit_form.url, "URL", {'label_attr': {'class': 'control-label'}}) }}

                    {# Affichage des erreurs pour ce champ précis. #}
                    {{ form_errors(edit_form.url) }}

                    <div class="col-sm-10">
                        {# Génération de l'input. #}
                        {{ form_widget(edit_form.url, {'attr': {'class': 'form-control'}}) }}
                    </div>
                </div>

                <div class="form-group">
                    {# Génération du label. #}
                    {{ form_label(edit_form.duration, "Durée", {'label_attr': {'class': 'control-label'}}) }}

                    {# Affichage des erreurs pour ce champ précis. #}
                    {{ form_errors(edit_form.duration) }}

                    <div class="col-sm-10">
                        {# Génération de l'input. #}
                        {{ form_widget(edit_form.duration) }}
                    </div>
                </div>

                <div class="form-group">
                    {# Génération du label. #}
                    {{ form_label(edit_form.description, "Description", {'label_attr': {'class': 'control-label'}}) }}

                    {# Affichage des erreurs pour ce champ précis. #}
                    {{ form_errors(edit_form.description) }}

                    <div class="col-sm-10">
                        {# Génération de l'input. #}
                        {{ form_widget(edit_form.description, {'attr': {'class': 'form-control'}}) }}
                    </div>
                </div>

                <div class="form-group">
                    {# Génération du label. #}
                    {{ form_label(edit_form.date, "Date", {'label_attr': {'class': 'control-label'}}) }}

                    {# Affichage des erreurs pour ce champ précis. #}
                    {{ form_errors(edit_form.date) }}

                    <div class="col-sm-10">
                        {# Génération de l'input. #}
                        {{ form_widget(edit_form.date) }}
                    </div>
                </div>

                <div class="form-group">
                    {# Génération du label. #}
                    {{ form_label(edit_form.poster, "Poster", {'label_attr': {'class': 'control-label'}}) }}

                    {# Affichage des erreurs pour ce champ précis. #}
                    {{ form_errors(edit_form.poster) }}

                    <div class="col-sm-10">
                        {# Génération de l'input. #}
                        {{ form_widget(edit_form.poster, {'attr': {'class': 'form-control'}}) }}
                    </div>
                </div>

                <div class="form-group">
                    {# Génération du label. #}
                    {{ form_label(edit_form.category, "Catégorie", {'label_attr': {'class': 'control-label'}}) }}

                    {# Affichage des erreurs pour ce champ précis. #}
                    {{ form_errors(edit_form.category) }}

                    <div class="col-sm-10">
                        {# Génération de l'input. #}
                        {{ form_widget(edit_form.category, {'attr': {'class': 'form-control'}}) }}
                    </div>
                </div>

                <div class="form-group">
                    {# Génération du label. #}
                    {{ form_label(edit_form.blocs, "Bloc", {'label_attr': {'class': 'control-label'}}) }}

                    {# Affichage des erreurs pour ce champ précis. #}
                    {{ form_errors(edit_form.blocs) }}

                    <div class="col-sm-10">
                        {# Génération de l'input. #}
                        {{ form_widget(edit_form.blocs, {'attr': {'class': 'form-control'}}) }}
                    </div>
                </div>


                    {{ form_widget(edit_form) }}
                    <input type="submit" value="Editer" />
                {{ form_end(edit_form) }}

                <ul>
                    <li>
                        <a href="{{ path('mmitv_video_index') }}">Retour à la liste des vidéos</a>
                    </li>
                    <li>
                        {{ form_start(delete_form) }}
                        <input type="submit" value="Supprimer">
                        {{ form_end(delete_form) }}
                    </li>
                </ul>
            </div>
        </div>
    </div>

{% endblock %}
